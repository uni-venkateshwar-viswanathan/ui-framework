import { css } from 'lit';

export const uniphoreToolbarVariableStyles = css`
  .uniphore-toolbar-item-variable {
    display: flex;
    flex-direction: row;
  }

  .session-type-icon,
  .value-type-icon {
    cursor: pointer;
    margin: 0px 2px;
  }
`;
