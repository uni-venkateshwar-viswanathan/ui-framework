import { css } from 'lit';
export const uniphoreIconStyle = css`
  :host {
    display: flex;
    flex-direction: column;
  }

  .uniphore-icon {
    color: var(--icon-color);
    font-size: var(--icon-size);
  }
`;
